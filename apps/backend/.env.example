PORT=4000
# TODO(открытый вопрос к заказчику, TASK_PATCH_1.md §5): часовой пояс, по которому cron
# (ScheduleResetService, @nestjs/schedule) считает "00:00" для ежедневного сброса расписания.
# По умолчанию UTC — подтвердить у заказчика реальный часовой пояс целевой аудитории перед
# продакшен-деплоем, если приложение не привязано к одному региону.
TZ=UTC
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/autism_connect?schema=public"
JWT_ACCESS_SECRET=change-me-access-secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_SECRET=change-me-refresh-secret
JWT_REFRESH_EXPIRES_IN=7d
FRONTEND_URL=http://localhost:3000
UPLOAD_DIR=./uploads/cards
PUBLIC_BASE_URL=http://localhost:4000

# Создание первого админ-аккаунта через seed (Part B.5) — не хардкодить учётные данные в коде.
ADMIN_EMAIL=admin@autismconnect.local
ADMIN_PASSWORD=wTCfm8Afp5OXC8A7
